# Foundry
contracts/out/
contracts/cache/
contracts/broadcast/

# Node
node_modules/
npm-debug.log*

# Backend runtime data (never commit the DB or uploaded images)
server/data/
server/package-lock.json

# Env / secrets — never commit keys
.env
.env.*
!.env.example

# Seeder generated deployer wallets — PRIVATE KEYS, never commit
seeder/wallets.json
seeder/bot-state.json
seeder/node_modules/

# Editor
.DS_Store
.idea/
.vscode/
scripts/node_modules/
